# SecureVault runtime dependencies (pure-Python where possible; no network).
cryptography>=41
segno>=1.5          # TOTP enrollment QR codes (dependency-free)
customtkinter>=5.2  # Aura design system: ttk/tk theming layer (MIT)
darkdetect>=0.8     # OS light/dark appearance + live change signal (BSD)
